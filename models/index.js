// import models
const User = require('./User');
const Poll = require('./Poll');
const Survey = require('./Survey');

// Not sure exactly if/how these fit in (especially with associations)
const Question = require('./Question');
const Answer = require('./Answer');

const { Model } = require('sequelize'); // Do I need this? Probably not

// make the stupid lame annoying associations here :(
